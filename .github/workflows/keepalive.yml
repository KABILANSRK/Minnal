name: Keep Render Alive

on:
  schedule:
    - cron: '*/12 * * * *'   # every 12 minutes
  workflow_dispatch: {}      # allows manual runs

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS --max-time 10 "https://minnal-u6z8.onrender.com" || true
